# Self-Supervised replay flow for Low-Resource Settings

Generated by FAROS experiment capability for the LLM workflow.

## Problem
replay flow requires large labeled datasets.

## Key Insight
Self-supervised pretraining can reduce label requirements.

## Structure
- `src/` runtime code
- `configs/` experiment configuration
- `scripts/` execution entrypoints
- `tests/` basic smoke tests
