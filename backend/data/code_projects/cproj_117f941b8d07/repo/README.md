# Scalable replay flow with Efficient Attention

Generated by FAROS experiment capability for the LLM workflow.

## Problem
Current replay flow methods do not scale to large datasets.

## Key Insight
Using sparse attention patterns can reduce complexity.

## Structure
- `src/` runtime code
- `configs/` experiment configuration
- `scripts/` execution entrypoints
- `tests/` basic smoke tests
